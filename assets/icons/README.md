# Icons

SVG and PNG icons loaded via `flutter_svg` / `Image.asset` go here.

This file keeps the directory in git, since `pubspec.yaml` lists
`assets/icons/` and the build fails if the directory is missing.
